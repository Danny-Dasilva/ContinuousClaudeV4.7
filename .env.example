# Copy to `.env` and fill in. `.env` is gitignored.

# Optional — used by tools/exa_search.py (web search bridge for the Ouros sandbox)
# Sign up at https://exa.ai
EXA_API_KEY=

# Optional — used by tools/nia_docs.py (documentation / indexed-source search)
# Sign up at https://nia.ai
NIA_API_KEY=

# Add any project-specific env vars below (DATABASE_URL, etc.)
